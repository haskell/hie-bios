cradle:
  bios:
    shell: |
      ECHO "-Wall" >> %HIE_BIOS_OUTPUT%
      ECHO "A" >> %HIE_BIOS_OUTPUT%
      ECHO "B" >> %HIE_BIOS_OUTPUT%
    dependency-shell: |
      ECHO "hie.yaml" >> %HIE_BIOS_OUTPUT%